; STUB CODE GENERATED BY CHATGPT
; IT DOESN'T WORK, CAN YOU IMAGINE

#include <avr/io.h>

    .section .text
    .global __start
    .org 0x0000

; Reset vector
    rjmp __start

__start:
    ; Set PB0 as output
    ldi r16, (1 << PB0)
    out DDRB, r16

loop:
    ; Toggle PB0
    in r17, PORTB
    eor r17, r16
    out PORTB, r17

    ; Dumb delay loop
    ldi r18, 0xFF
delay1:
    ldi r19, 0xFF
delay2:
    nop
    dec r19
    brne delay2
    dec r18
    brne delay1

    rjmp loop
